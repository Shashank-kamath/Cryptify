# Cryptify
Cryptify is a web based application which can convert plain text(user input) into a cipher text(encrypted text) and vice versa using desired cryptographic encryption decryption algorithms.
